# SYNORA

SYNORA 스마트 글라스 컴패니언 앱 모바일 프로토타입입니다.

## 실행 방법

가장 간단한 방법:

1. `index.html`을 브라우저에서 바로 엽니다.

로컬 서버로 실행하려면:

1. 이 폴더에서 `python -m http.server 4173` 실행
2. 브라우저에서 `http://localhost:4173` 열기

## 파일

- `index.html`
- `styles.css`
- `script.js`
